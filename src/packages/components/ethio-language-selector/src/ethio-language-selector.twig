<div class="ethio-language-container">
  <button type="button" class="ethio-button{{ focus ? " ethio-focus" }}" {% if role %} role="{{ role }}" {% endif %}>
    <span lang="{{ lang }}" xml:lang="{{ lang }}">{{ label | default('Español') }}</span>
  </button>
</div>
