<div class="ethio-overlay"></div>
<header class="ethio-header ethio-header--basic {{ modifier }}">
  <div class="ethio-nav-container">

    <div class="ethio-navbar">
      {% include "@components/ethio-site-title/src/ethio-site-title.twig" %}

      <div class="ethio-language-container ethio-language--small display-block desktop:display-none">
        <ul class="ethio-language__primary ethio-accordion">
          <li class="ethio-language__primary-item">
            <button type="button" class="ethio-button ethio-button--unstyled ethio-language__link{{ focus ? " ethio-focus" }}" {% if role %} role="{{ role }}" {% endif %} aria-expanded="false" aria-controls="language-options-mobile">
              {{ label | default('Languages') }}
            </button>
            {% if languages %}
              <ul id="language-options-mobile" class="ethio-language__submenu" hidden="true">
                {% for language in languages %}
                  <li class="ethio-language__submenu-item">
                    <a href="{{ placeholder_link }}">
                      {% if language.lang_label %}
                        <span lang="{{ language.lang_att }}" xml:lang="{{ language.lang_att }}">
                          <strong>{{ language.lang_label }}</strong>
                          {{ language.lang_label_en }}
                        </span>
                      {% endif %}
                    </a>
                  </li>
                {% endfor %}
                <li class="ethio-language__submenu-item">
                  <a href="{{ placeholder_link }}">{{ more }}</a>
                </li>
              </ul>
            {% endif %}
          </li>
        </ul>
      </div>

      {% if nav %}
        <button class="ethio-menu-btn">
          {{ nav.menu.label | default('Menu') }}
        </button>
      {% endif %}
    </div>

    {% if nav %}
      <nav aria-label="{{ nav.aria_label }}" class="ethio-nav">
        {% include "@components/ethio-nav/src/ethio-nav__primary/ethio-nav__primary.twig" %}
        {% include "@components/ethio-search/src/ethio-search.twig" with search.settings %}
      </nav>
    {% endif %}

    <div class="ethio-language-container ethio-language--small display-none desktop:display-block">
      <ul class="ethio-language__primary ethio-accordion">
        <li class="ethio-language__primary-item">
          <button type="button" class="ethio-button ethio-language__link{{ focus ? " ethio-focus" }}" {% if role %} role="{{ role }}" {% endif %} aria-expanded="false" aria-controls="language-options">
            {{ label | default('Languages') }}
          </button>
          {% if languages %}
            <ul id="language-options" class="ethio-language__submenu" hidden="true">
              {% for language in languages %}
                <li class="ethio-language__submenu-item">
                  <a href="{{ placeholder_link }}">
                    {% if language.lang_label %}
                      <span lang="{{ language.lang_att }}" xml:lang="{{ language.lang_att }}">
                        <strong>{{ language.lang_label }}</strong>
                        {{ language.lang_label_en }}
                      </span>
                    {% endif %}
                  </a>
                </li>
              {% endfor %}
              <li class="ethio-language__submenu-item">
                <a href="{{ placeholder_link }}">{{ more }}</a>
              </li>
            </ul>
          {% endif %}
        </li>
      </ul>
    </div>
  </div>
</header>
